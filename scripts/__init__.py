# The scripts are run as files rather than imported, but the parts of them
# that are worth testing on their own are modules, and a module is only
# discoverable by `python -m unittest` from a package. That is all this is for.
